on: ["push"]

jobs:
  test:
    runs-on: docker
    container:
      image: gradle:alpine

    steps:
      - name: test
        run: |
          export GRADLE_USER_HOME=`pwd`/.gradle
          cat ./settings.gradle

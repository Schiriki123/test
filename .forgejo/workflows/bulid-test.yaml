on: ["push"]

jobs:
  test:
    runs-on: docker
    container:
      image: gradle:alpine

    steps:
      - name: test
        run: |
          echo "${ls}"
          export GRADLE_USER_HOME=`pwd`/.gradle

